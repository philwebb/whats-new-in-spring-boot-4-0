server:
  port: 8081

spring:
  application:
    name: isdb-web
  docker:
    compose:
      lifecycle-management: start-only
  http:
    serviceclient:
      stuffie:
        base-url: 'http://localhost:8080'
        read-timeout: 1s
        apiversion:
          insert:
            header: API-Version

management:
  tracing:
    sampling:
      probability: 1.0
